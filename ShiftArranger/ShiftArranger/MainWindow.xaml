<Window x:Class="ShiftArranger.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ShiftArranger"
        mc:Ignorable="d"
        Title="MainWindow" Height="515.789" Width="1200">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="3*"/>
            <RowDefinition Height="1*"/>
        </Grid.RowDefinitions>
        <StackPanel Grid.Row="0" ScrollViewer.VerticalScrollBarVisibility="Disabled">
            <TextBlock>Doctors</TextBlock>
            <ListView x:Name="DoctorListView"
                ScrollViewer.HorizontalScrollBarVisibility="Disabled" ScrollViewer.VerticalScrollBarVisibility="Visible" >
                <ListView.View>
                    <GridView>
                        <GridViewColumn Header="ID" DisplayMemberBinding="{Binding ID}"/>
                        <GridViewColumn Header="Type"  DisplayMemberBinding="{Binding doctorType}"/>
                        <GridViewColumn Header="Main Ward"  DisplayMemberBinding="{Binding mainWard}"/>
                        <GridViewColumn Header="Available Ward"  DisplayMemberBinding="{Binding capableOf}"/>
                        <GridViewColumn Header="Want"  DisplayMemberBinding="{Binding absoluteWantThisDay}"/>
                        <GridViewColumn Header="Avoid"  DisplayMemberBinding="{Binding absoluteAvoidThisDay}"/>
                        <GridViewColumn Header="Relative Want"  DisplayMemberBinding="{Binding relativeWantThisDay}"/>
                        <GridViewColumn Header="Relative Avoid"  DisplayMemberBinding="{Binding relativeAvoidThisDay}"/>
                    </GridView>
                </ListView.View>
            </ListView>
        </StackPanel>
        <StackPanel Grid.Row="1">
            <Button Click="Button_Click" Content="Do" />
        </StackPanel>
    </Grid>
</Window>
